# 邮箱验证码发信配置(模板)
#
# 用法:把本文件复制为 smtp.env 并填入真实值,然后双击项目根目录的 start-server-smtp.bat 启动。
#   - smtp.env 已被 .gitignore 忽略,不会进版本库,交作业/推代码都不会泄露;
#   - 本模板文件是给人看的样例,里面没有任何真实密钥,可以安全提交。
#
# 不填也能用:验证码会打印在服务端控制台(见 server/mailer.py 的兜底逻辑)。
#
# ── QQ 邮箱 ────────────────────────────────────────────────
# 1. 登录 https://mail.qq.com → 设置 → 账户
# 2. 找到「POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务」→ 开启「SMTP服务」
# 3. 按提示用手机发一条短信验证 → 页面给出一串 16 位「授权码」
# 4. 下面 XIANGSU_SMTP_PASS 填这串授权码(不是 QQ 密码!)
#
# ── 163 邮箱 ───────────────────────────────────────────────
# HOST=smtp.163.com  PORT=465  SSL=1,同样使用「客户端授权密码」

XIANGSU_SMTP_HOST=smtp.qq.com
XIANGSU_SMTP_PORT=465
XIANGSU_SMTP_SSL=1
XIANGSU_SMTP_USER=你的邮箱@qq.com
XIANGSU_SMTP_PASS=你的16位授权码
XIANGSU_SMTP_NAME=像素填空

# 演示用开关:设为 1 时,发码接口会把验证码一并返回,登录页直接显示。
# 方便答辩现场(不用查控制台、不用收邮件),但会让任何人都能拿到验证码,
# 所以只在本地演示时开,正式部署务必保持 0 或删掉这一行。
# XIANGSU_ECHO_CODE=0
