# 桂林电子科技大学 202 -202 学年第 学期

# 《移动应用开发》作品说明书

**课号：** 　　　　　　　　**任课老师：** 刘斯韵

|  工作量 |    学号    |  姓名  |
| :--: | :------: | :--: |
| 　　　% | （组长）　　　　 | 　 　　 |
| 　　　% | （组员）　　　　 | 　 　　 |
| 　　　% | （组员）　　　　 | 　 　　 |

|    项目    | 内容                     |    项目    | 内容                   |
| :------: | :--------------------- | :------: | :------------------- |
| **项目名称** | 像素填空（Fill-a-Pix）逻辑解谜游戏 |  **院系**  | 计算机与信息安全学院           |
|  **专业**  | 　　　　                   |  **平台**  | Android 原生 App       |
|  **学号**  | 　　　　                   |  **姓名**  | 　　　　                 |
| **实验日期** | 202 年 月 日              | **开发语言** | Kotlin（+ Python 工具链） |

> 说明：本文为 Markdown 源稿，粘贴至 Word 后请按报告要求排版（正文宋体小四、行间距 20 磅；标题三级、四号黑体、顶格；图题在图下方居中，表题在表上方居中），并将文中"图 x.x"占位处替换为实际运行截图。

***

## 一．实验目的

1、掌握中小型项目的需求分析、系统设计、编码、测试，以及项目的部署运行，具备复杂工程问题的解决能力；

2、掌握综合应用开发知识点进行管理和软件开发的能力；

3、掌握文献检索、系统建模、文档写作等方面的能力；

4、综合运用所学知识，结合实习需求完成项目开发。

***

## 二．实验内容

每组 2~~5 人（建议 2~~3 人）合作，完成运行于 Android 平台的康思谜题（Conceptis Puzzles）类原生 App 的设计开发以及综合实验报告。评分维度包括：1、能否算法解题；2、规则理解是否到位；3、能否生成游戏；4、特色；5、每道游戏是否确定唯一解；同时参照界面设计、控件与布局使用、数据持久化、多线程等实现情况给分。游戏从网络获取、排行榜、网络与蓝牙对战、自动生成游戏、微信小程序版等属于加分项。报告需包含选题、需求分析、概要设计（含数据库的概念 / 逻辑 / 物理设计）、详细设计、系统实现、系统测试、存在的问题及思路、总结。

***

## 三．选题

### 3.1 系统简介

本作品为 Android 平台的**像素填空（Fill-a-Pix）逻辑解谜游戏**，取材自康思谜题系列。游戏规则：棋盘上部分格子标有数字提示，每个数字表示**以该格为中心的 3×3 区域内需要涂黑的格子数量**（区域越出棋盘边缘的部分不计）。玩家通过纯逻辑推理逐格确定"涂黑"与"留白"，最终把整个棋盘还原成一幅完整的像素画。

系统内置 10 个关卡，图案依次为红心、笑脸、蝴蝶、苹果、小房子、小鱼、星星、皇冠、月亮、火箭，尺寸从 10×10 递进到 28×28，分简单（1 星）、中等（2 星）、困难（3 星）三档难度。每关谜面均经过自研求解器（约束传播 + 回溯）校验，保证**有且仅有唯一解**。

完整功能体系包括：

- **三页式登录**：品牌入口页分流 → 玩家专属页（免密、进度总览）与管理员专属页（独立密码验证）完全分离；

- **双身份权限**：玩家按顺序解锁关卡（通关第 N 关解锁第 N+1 关）；管理员（默认密码 admin）全局解锁全部关卡；

- **双模式**：自由模式不限时全局计时；限时模式按难度配置 180\~720 秒时限，超时判负、倒计时低于 1 分钟变红警示；

- **棋盘交互**：智能双向涂黑笔（点空白格涂黑、点已黑格清除，起笔第一格决定整笔方向）、留白标记笔、拖动连涂、双指缩放平移、＋/−缩放按钮与复位；

- **笔划级撤销 / 重做 / 重置**：一条拖动笔划或一次批量操作（一键补齐、清除错误格）均合并为一步可撤销操作；

- **三级辅助体系**：显式检查错误（点击才精确标红真正画错的格子，可一键清除）、一键补齐（全局额度制：每次登录 +2、上限 6、跨登录累积）、参考答案全屏查看；

- **游戏化结算**：通关揭晓完整像素画 + 撒花动画 + 用时统计 + 下一关直达；限时超时弹出失败结算；

- **持久化**：通关进度与补齐额度本地保存，杀进程重启不丢失；

- **深夜画室主题**：恒定深色界面（夜幕底 + 亮纸棋盘的聚光对比），品牌靛紫 / 湖水青 / 暖金三色体系，渐变 Hero、分段控件、玻璃质感关卡卡贯穿全 app；竖屏手机与宽屏（≥600dp 平板 / 横屏）自适应双布局。

### 3.2 选题理由

1. **规则极简、推理极深**。Fill-a-Pix 的规则一句话就能讲清（3×3 窗口计数），但求解需要综合运用排除法、区域分割、边界推理、计数归约等多种逻辑手段，"易学难精"的曲线非常适合闯关制游戏，也让"算法解题"这一评分点有真正的用武之地——不是摆一个求解器做样子，而是求解结果直接决定关卡质量。

2. **规则理解可被程序严格验证**。Fill-a-Pix 的规则是纯数学约束：提示数字、答案矩阵、解的唯一性都可以被程序精确表达和验证。本项目每关谜面都由求解器证明唯一解，并由 16 项单元测试在每次构建时自动重新证明，"每道游戏是否确定唯一解"这一评分点不靠人工检查，而是构建流水线强制把关。

3. **具备游戏自动生成的完整管线**。实现了"答案像素图 → 提示推导（3×3 窗口计数）→ 贪心去提示（保留唯一解前提下尽量精简）→ 唯一解校验 → 稀疏谜面"的全自动生成链路，Kotlin 端与 Python 端各一套。新关卡的制作成本从"手工设计并人工验证"降低到"画一幅像素图 + 跑一次脚本"，具备评分点 3"能否生成游戏"的完整能力。

4. **结果有画面感，天然适合展示**。数独类谜题的终点是一堆数字，而 Fill-a-Pix 的终点是一幅像素画——通关瞬间棋盘隐藏全部数字、直接呈现完整图案并撒花庆祝，游戏性和答辩演示效果兼备。

5. **工程结构有代表性**。项目体量适中但覆盖面广：算法层（生成与求解）、状态层（笔划 / 双栈）、渲染层（Canvas 自绘）、手势层（多指针识别与过滤）、持久化（SharedPreferences）、主题系统（Material 3 恒定深色）、单元测试（纯 JVM 毫秒级）、开发工具链（Python），是"五脏俱全"的中小型项目，能有效锻炼分层设计与工程化能力。

### 3.3 技术选型

| 维度  | 选型                                              | 理由                                  |
| --- | ----------------------------------------------- | ----------------------------------- |
| 语言  | Kotlin 2.2.10                                   | Android 官方一等公民；空安全、数据类、委托属性适合状态建模   |
| UI  | Jetpack Compose + Material 3                    | 声明式 UI，状态驱动重组；单 Canvas 棋盘渲染性能好      |
| 构建  | AGP 9.2.1 / Gradle Kotlin DSL / Version Catalog | 依赖集中管理，配置可读                         |
| SDK | minSdk 24 / targetSdk 36 / compileSdk 36        | 覆盖 Android 7.0 以上绝大多数设备             |
| 持久化 | SharedPreferences                               | 数据量为"通关集合 + 一个整数"，键值存储最合适，无需 SQLite |
| 测试  | JUnit 4（纯 JVM）                                  | 逻辑层零 Android 依赖，测试毫秒级运行             |
| 工具链 | Python 3                                        | 快速编写图案渲染 / 提示生成 / 校验脚本              |

***

## 四．需求分析

### 4.1 功能需求

按"身份登录 / 关卡模式 / 棋盘交互 / 辅助体系 / 系统数据"五组归纳。

**（1）身份与登录**

|  编号  | 需求                                                     |
| :--: | ------------------------------------------------------ |
| FR-1 | 启动进入登录入口页：品牌渐变 Hero（含真实关卡像素画装饰与浮动动画），提供玩家 / 管理员两条入口    |
| FR-2 | 玩家免密进入专属页，展示通关进度（渐变进度条）与一键补齐额度，点击"开始游戏"进入首页            |
| FR-3 | 管理员进入独立登录页，密码验证（默认 admin）通过后进入首页；密码错误即时提示；支持键盘"前往"直接登录 |
| FR-4 | 玩家顺序解锁（通关第 N 关解锁第 N+1 关，第 1 关恒解锁）；管理员全局解锁              |
| FR-5 | 首页可退出登录返回入口页                                           |

**（2）关卡与模式**

|   编号  | 需求                                                          |
| :---: | ----------------------------------------------------------- |
|  FR-6 | 首页网格展示全部关卡：编号徽章、难度星级、尺寸、锁定状态；通关的点亮描边并揭晓像素画与图案名，未通关显示深色轮廓"？" |
|  FR-7 | 分段控件切换自由 / 限时模式，选择结果带入游戏页                                   |
|  FR-8 | 自由模式不限时，全局计时（mm:ss），通关 / 退出 / 重开时停止归零                       |
|  FR-9 | 限时模式按难度配 180\~720s 时限，超时判负弹失败结算且不写通关进度；倒计时低于 60s 变红         |
| FR-10 | 通关弹胜利结算：图案揭晓、用时统计、下一关 / 再来一局 / 返回列表                         |

**（3）棋盘交互**

|   编号  | 需求                                                      |
| :---: | ------------------------------------------------------- |
| FR-11 | 两支工具笔：智能双向涂黑笔与留白笔，分段控件切换                                |
| FR-12 | 涂黑笔单击：落在未定 / 留白格 → 涂黑；落在已黑格 → 清除；起笔第一格决定整笔方向，拖动过程方向保持一致 |
| FR-13 | 按住拖动沿轨迹连续涂色（快速滑动不断线），一条笔划内所有改动合并为一步可撤销操作                |
| FR-14 | 双指捏合缩放 + 拖动平移；双指落下的瞬间自动取消第一指已产生的误涂                      |
| FR-15 | 大棋盘（≥200 格）右下角 ＋/− 缩放按钮，右上角复位；缩放平移不会把棋盘完全拖出视野           |
| FR-16 | 桌面环境：仅鼠标左键可起笔 / 拖涂；滚轮、右键、中键一律不触发任何棋盘操作                  |
| FR-17 | 撤销 / 重做 / 重置：标准双栈语义，新操作清空重做栈；重置清空棋盘并归零计时                |
| FR-18 | 进度条实时显示"已涂 X / 应涂 N"；提示数字满足条件时变绿                        |

**（4）辅助体系**

|   编号  | 需求                                                                    |
| :---: | --------------------------------------------------------------------- |
| FR-19 | 检查错误：点击才触发，按答案精确标出"已提交但画错"的格子（红底红 ✗）并显示错误数；未决定的格子永不误报；可一键清除错误格（整批可撤销） |
| FR-20 | 一键补齐：自动补全本关全部正确像素；全局额度制（登录 +2、上限 6、跨登录累积、全关共用），额度用尽按钮灰化               |
| FR-21 | 参考答案：全屏查看完整像素画（支持缩放），看过会在结算中标注                                        |

**（5）系统与数据**

|   编号  | 需求                                                         |
| :---: | ---------------------------------------------------------- |
| FR-22 | 通关进度与补齐额度本地持久化，杀进程 / 重启不丢失                                 |
| FR-23 | 关卡表调整后自动清理存档中的孤儿关卡 id，避免统计污染                               |
| FR-24 | 布局自适应：竖屏手机（棋盘最大化 + 紧凑控件）；宽屏 ≥600dp（左棋盘 + 右侧栏）；结算卡随屏高自适应不溢出 |
| FR-25 | 恒定深色"深夜画室"主题，不随系统亮暗切换，任何设备观感一致                             |

### 4.2 非功能需求

|   编号  | 类别   | 需求                                               |
| :---: | ---- | ------------------------------------------------ |
| NFR-1 | 正确性  | 每关谜面有且仅有唯一解且与答案一致，由单元测试在构建时强制把关                  |
| NFR-2 | 性能   | 28×28（784 格）单 Canvas 一次绘制；状态变更经版本号驱动局部重组，连涂缩放不掉帧 |
| NFR-3 | 可测试性 | 逻辑层零 Android 依赖，JVM 上秒级完成全部单元测试                  |
| NFR-4 | 可维护性 | 逻辑与 UI 严格分层；关卡数据、提示位置、工具脚本三处同步有标准流程              |
| NFR-5 | 可用性  | 关键操作有即时视觉反馈；错误提示不打断心流（默认不标错）                     |
| NFR-6 | 兼容性  | minSdk 24（Android 7.0）起；系统栏适配恒定深色观感              |

### 4.3 核心用例

主要角色为**玩家**与**管理员**。玩家主流程：选择身份 → 查看进度 → 选模式 → 选关卡 → 涂格推理（涂黑 / 清除 / 留白 / 连涂 / 缩放）→ 撤销重做 → （扩展）检查错误 / 一键补齐 / 参考答案 → 通关结算 → 解锁下一关。管理员流程：独立页验证密码 → 全部关卡直接可玩，其余与玩家一致。核心用例"涂格推理"的主场景为：选中涂黑笔 → 空白格按下（涂黑）→ 拖动经过多格（连续涂黑）→ 抬起（笔划入撤销栈）；扩展场景：按下时第二指落下（取消笔划）、落在已黑格（本笔转为清除方向）、通关或超时后（棋盘冻结仅可查看）。

***

## 五．概要设计

### 5.1 总体架构

系统采用\*\*逻辑层（model）— 界面层（ui）— 持久化层（progress）\*\*三层架构，逻辑层与 Android 框架完全解耦。总体结构如图 5.1（文字结构图，正式报告中可替换为绘图）。

```
┌─────────────────────────────────────────────────────────────┐
│                     界面层 ui（Compose）                      │
│  LoginScreen(三页)   HomeScreen   GameScreen   BoardView     │
│  Widgets(分段控件/渐变按钮/玻璃胶囊)   theme(深夜画室主题)      │
└───────────────△─────────────────────────────△───────────────┘
                │ 回调(onStroke*/onClick)      │ 状态读取
┌───────────────┴─────────────────────────────┴───────────────┐
│                      逻辑层 model（纯 Kotlin）                │
│  CellState 三态    Level/Levels/SparseClues 关卡数据          │
│  GameBoard 笔划+双栈    PuzzleGenerator 提示推导              │
│  PuzzleSolver 约束传播+回溯    Validator 三类校验             │
└───────────────△─────────────────────────────△───────────────┘
                │ markCompleted/useFill        │ 启动加载/清理
┌───────────────┴─────────────────────────────┴───────────────┐
│              持久化层 GameProgress（SharedPreferences）        │
└─────────────────────────────────────────────────────────────┘

配套工具链 tools/（Python）：render / gen / verify / update clues
```

图 5.1 系统总体架构分层图

分层的收益直接体现在测试与复用上：`PuzzleSolver`、`GameBoard`、`Validator` 等纯逻辑类不依赖任何 Android API，16 项单元测试在 JVM 上 2 秒内完成；未来开发鸿蒙端或小程序端时，**逻辑层可原样复用，只需重写界面层**。

### 5.2 模块划分与职责

| 模块   | 文件                                | 职责                                            |
| ---- | --------------------------------- | --------------------------------------------- |
| 数据模型 | model/CellState、UserRole、GameMode | 三态枚举（未定/涂黑/留白）、身份、模式                          |
| 关卡数据 | model/Level、Levels、SparseClues    | 关卡结构、10 个内置关、稀疏提示位置                           |
| 棋盘状态 | model/GameBoard                   | 格子矩阵 + 笔划机制 + 撤销/重做双栈 + 批量操作                  |
| 谜面生成 | model/PuzzleGenerator             | 答案 → 全量提示；贪心去提示 → 稀疏谜面                        |
| 求解器  | model/PuzzleSolver                | 约束传播 + 回溯，判定唯一解                               |
| 校验器  | model/Validator                   | 胜利判定 / 错误检测 / 已满足提示                           |
| 登录界面 | ui/LoginScreen                    | 入口页 + 玩家专属页 + 管理员专属页                          |
| 首页   | ui/HomeScreen                     | 渐变 Hero、模式分段控件、关卡网格                           |
| 游戏页  | ui/GameScreen                     | HUD、计时、限时逻辑、辅助操作、结算、响应式布局                     |
| 棋盘视图 | ui/BoardView                      | Canvas 棋盘渲染、单指笔划、双指变换、指针过滤、像素画预览              |
| 共享控件 | ui/Widgets                        | SegmentedControl / GradientButton / GlassChip |
| 主题   | ui/theme                          | 深夜画室配色、Material 3 色板、字体排版                     |
| 持久化  | GameProgress                      | 通关集合、顺序解锁、额度管理、登录奖励                           |
| 导航   | MainActivity                      | Screen 密封接口状态机式页面切换                           |

页面导航采用密封接口状态机（页面少、跳转简单，无需引入 Navigation 组件）：`Login(入口) → PlayerLogin(玩家页) / AdminLogin(管理员页) → Home(身份) → Game(关卡, 模式)`，通关后可在 Game 内原地切换下一关；`when` 分支穷举保证导航类型安全。

### 5.3 数据库设计

本系统持久化数据只有两类：**通关进度集合**与**一键补齐额度计数**，数据量为"一个集合 + 一个整数"。经权衡采用 Android 轻量键值存储 SharedPreferences（XML 文件）而非 SQLite——引入数据库属于过度设计。以下按概念 / 逻辑 / 物理三层描述。

**（1）概念设计（E-R 模型）**：涉及两个实体、一个联系。实体"玩家进度"，属性：已通关关卡 id 集合、已消耗补齐次数；实体"关卡"，属性：id、名称、尺寸、难度、时限、提示矩阵、答案矩阵（关卡本体为编译期常量，固化在 Levels.kt 中，不参与运行时持久化）；联系"通关"：玩家进度与关卡之间的多对多联系，记录于进度实体的通关集合中。

**（2）逻辑设计（关系模式）**：进度表 Progress(completedLevelIds: Set\<Int>, fillQuotaUsed: Int)，单机单用户即全局唯一一行；关卡表 Level(levelId 主键, name, rows, cols, difficulty, timeLimit, clueGrid, answerGrid) 为静态常量表不落库。映射到键值存储为两个键值对：`completed_levels`（Set\<String>，存 "1","2",…）与 `fill_quota_used`（Int，0..6）。未来若引入网络关卡，Level 序列化为 JSON 落入 SQLite 或远端即可，逻辑层结构无需改动。

**（3）物理设计（存储实现）**：物理上对应应用私有目录下单个 XML 文件 `/data/data/com.example.xiangsugame/shared_prefs/xiangular_progress.xml`，内容为通关 id 集合标签与一个整数值。访问策略：`GameProgress` 以 `MODE_PRIVATE` 打开；写操作一律 `edit().apply()` 异步落盘避免阻塞主线程；读操作在构造时一次性加载并映射为 Compose 可观察状态（mutableStateOf），首页与游戏页直接订阅状态自动重组，无重复 I/O。额度上限（6）与登录奖励（+2）为业务常量，固化在伴生对象中。

### 5.4 核心算法概要设计

1. **提示推导（答案 → 谜面）**：`clue[r][c]` = 以 (r,c) 为中心的 3×3 窗口内答案涂黑格数量，越界部分不计，复杂度 O(rows×cols×9)。
2. **求解器（谜面 → 解集）**：约束传播 + 回溯的深度优先搜索。传播规则 A：窗口内已涂黑数等于提示值且仍有未定格 → 未定格全部置空；规则 B：提示值减已涂黑数等于未定格数 → 未定格全部涂黑。两规则反复应用至不动点后仍有未定格则分支递归；解数达 2 立即中止（判定非唯一）。
3. **稀疏提示生成（谜面美化）**：从全量提示出发贪心逐个尝试删除，删除后调用求解器校验仍唯一解则保留删除，否则回滚。一轮贪心即可得到大幅精简且唯一解不变的谜面。
4. **笔划机制（交互打包）**：startStroke → strokeCell×N → commitStroke 把一次拖动的 N 个改动聚合为单个 Move.Batch 入撤销栈；cancelStroke（双指落下等异常结束）整体回滚。

***

## 六．详细设计

### 6.1 数据模型

**格子三态**（CellState）以 Int 值存储，便于 IntArray 紧凑表示整盘：UNDECIDED(0) 未决定（暖白纸面）、FILLED(1) 涂黑（深墨紫）、MARKED\_EMPTY(2) 标记确定留白（浅薰衣草底 + 圆点）。

**关卡结构**（Level）：id / name / difficulty(简单★/中等★★/困难★★★) / rows / cols / clueGrid（−1 表示无提示）/ answerGrid / timedLimitSeconds / cluePositions（稀疏提示坐标集）。工厂方法 `Level.fromAnswer()` 接收答案矩阵与提示位置，内部调用 PuzzleGenerator 生成 clueGrid——**谜面是推导产物而非手工数据，天然与答案一致**。

**关卡数据组织**：小图（10×10~~16×16）用 ASCII 字符画~~ ~~`art("X.", …)`~~ ~~定义，直观易改；大图（20×20~~28×28）用 fillRect / fillEllipse / carveEllipse / fillTriangle 程序化作画（如月亮 = 大圆挖去偏置小圆、星星 = 手写五角星）。

### 6.2 棋盘状态与笔划机制（GameBoard）

```
class GameBoard(rows, cols) {
    cells: IntArray                          // 当前棋盘（三态 Int）
    undoStack / redoStack: ArrayDeque<Move>  // 双指令栈

    startStroke()                        // 开笔：暂存本笔改动
    strokeCell(r,c,target): Boolean      // 涂格：目标态相同则跳过，返回是否变化
    commitStroke()                       // 收笔：非空笔划打包 Batch 压栈（清空 redo）
    cancelStroke()                       // 取消：回滚本笔全部改动

    undo() / redo() / reset()            // 标准栈语义；重置可撤销
    applySolution(answer)                // 一键补齐：整批 Batch
}
```

Move 抽象为"一批 (格子下标, 旧值, 新值)"三元组集合：撤销 = 逆序回写旧值，重做 = 正序回写新值。三个设计要点：**一笔一记录**（拖涂 20 格只占一个栈元素，撤销一步整笔回退）；**cancel 与 commit 对称**（手势层任何非正常结束都整体回滚，不留半笔）；strokeCell 返回 Boolean 供界面层驱动版本号，避免无效重组。

### 6.3 求解器（PuzzleSolver）

求解主流程：初始化每格值域 {0,1} → 循环应用传播规则 A/B 至不动点 → 全部确定则记录一个解 → 否则任选未定格分支（假设 1 / 0 递归）→ 解数达到 maxSolutions 提前终止。约束传播先收敛大部分格子，回溯只处理真正歧义区域，28×28 谜面毫秒级出解。`maxSolutions=2` 的模式即"唯一解判定器"，是关卡生成的质量闸门；同时它也是"算法解题"能力的直接体现——给定任意合法谜面，程序都能推出全部解。

### 6.4 校验器（Validator）

三类校验相互独立：`isSolved`（每格 FILLED 状态与答案逐位相等，留白标记不参与——通关判定）；`wrongCells`（仅报"已提交但画错"：FILLED≠答案 1 或 MARKED\_EMPTY≠答案 0，UNDECIDED 永不误报——检查错误）；`satisfiedClues`（提示格窗口无未定格且涂黑数等于提示值——数字变绿反馈）。"错误检查默认不打扰"的设计哲学：平时棋盘不显示任何对错信息（防止变相偷看答案），玩家主动请求时才一次性给出精确结果。

### 6.5 棋盘视图与手势（BoardView）

**渲染**：单 Canvas 自绘整盘（28×28 也只是一次绘制批次）。绘制顺序：格子底色（图片模式墨黑 > 错误红底 > 涂黑 > 留白 > 空白五级分色）→ 留白圆点 → 提示数字（按数值 0\~9 分色、已满足变绿、涂黑格上白字）→ 错误红 ✗ → 网格线（线宽 ÷ viewScale，放大后仍是 1px）→ 装裱外框（随缩放变换）。

**手势**（awaitEachGesture 自定义循环）：awaitFirstDown 等待落下 → **指针类型过滤**（仅 Touch 与鼠标左键进入笔划流程，中键 / 右键 / 触控笔 / 滚轮忽略）→ 单指进入笔划采样循环（移动量超半格步长即换算格子回调 onStrokeCell，快速滑动不断线）→ 第二指落下立即 onStrokeCancel 回滚误涂并切换缩放平移 → 抬起 onStrokeEnd 提交笔划；非交互模式（通关揭晓 / 超时冻结）仅允许查看手势。**缩放体系**：双指捏合（手势中心锚定）+ ＋/− 按钮（视口中心锚定）+ 复位；clampViewOffset 把平移量约束在"棋盘始终覆盖视口"内，杜绝拖飞。指针坐标到格子下标为 O(1) 逆变换。

### 6.6 游戏页状态管理（GameScreen）

界面状态全部为 Compose 可观察对象：boardVersion（Int 计数器，棋盘 Canvas 重组的唯一触发器，避免整页重组）、tool / strokeTarget（当前笔与智能笔整笔方向）、checkedWrong（检查结果，动手即清空）、elapsedSeconds / timedOut（计时与超时）、showZoomed / revealedAnswer（参考答案）。

智能双向笔方向判定：onStrokeBegin 时 strokeTarget 置空，本笔第一格落在黑格则整笔为清除，否则整笔为涂黑——拖动过程方向一致，撤销依然一步整笔。计时用 LaunchedEffect + delay(1s) 协程自增；限时超时置 timedOut 冻结棋盘弹失败卡。响应式布局用 BoxWithConstraints 判定宽 ≥600dp 走"左棋盘（封顶 480dp 居中）+ 右侧 230dp 竖排控制栏"，否则竖排紧凑布局（控件压缩至约 130dp，棋盘最大化）；结算卡 heightIn(max = maxHeight−24dp) + 卡内 verticalScroll，任何屏幕不溢出。

### 6.7 进度持久化（GameProgress）

completedLevels 与 fillQuotaUsed 均为 mutableStateOf，首页关卡网格与游戏页额度按钮实时联动；markCompleted 幂等写；pruneCompleted 在启动时清理已删关卡孤儿 id；grantLoginBonus 每次登录额度 +2（满额 6 幂等）；useFill 扣减额度（用尽返回 false）；顺序解锁判定 `isUnlocked(id) = id==1 || (id-1) in completed`，管理员身份在界面层绕过。

### 6.8 视觉主题与共享控件

**深夜画室主题**：夜幕背景 #100E1C + 夜表面卡片 #1B1929，品牌三色（靛紫 #8B7CFF / 湖水青 #5EDDD8 / 暖金 #FFC85C）；棋盘刻意保持亮色纸面，与暗界面形成"聚光"对比突出玩法区；MainActivity 固定 SystemBarStyle.dark 浅色系统栏图标，任何系统外观下观感一致。**Widgets.kt 共享控件**：SegmentedControl\<T>（泛型分段选择器，选中段渐变高亮）、GradientButton（渐变主按钮）、GlassChip（Hero 玻璃胶囊），登录 / 首页 / 游戏三端复用保证风格统一。**PicturePreview** 把任意答案矩阵渲染为透明底小图（首页谜底、结算揭晓、登录 Hero 装饰通用）。

### 6.9 Python 工具链

四个脚本构成关卡生产线：render\_patterns.py / final\_patterns.py（图案渲染成终端字符画，肉眼核对造型与对称性）；gen\_clues.py（贪心去提示 + 唯一解校验，输出 Kotlin SparseClues 代码段）；verify\_clues.py（解析 SparseClues.kt 逐关校验唯一解）；update\_clues.py（安全写回）。修改关卡标准流程：改 Levels.kt 图案 → 同步 gen\_clues.py 图案副本 → verify 定位失效关 → gen 重生成 → update 写回 → 单元测试终审。

***

## 七．系统实现

### 7.1 工程与技术栈

Kotlin 2.2.10 + Jetpack Compose（BOM 统一版本）+ Material 3；AGP 9.2.1 / Gradle Kotlin DSL / libs.versions.toml Version Catalog；minSdk 24 / targetSdk 36；JUnit 4 纯 JVM 测试；Python 3 工具链。

### 7.2 关键功能实现

1. **通关判定与结算**：每次棋盘版本号变化后调用 Validator.isSolved；为 true 时冻结棋盘、切换"图片模式"（隐藏提示数字，直接以答案矩阵渲染，避免留白圆点残留在成品画上）、撒花动画 + 三色渐变描边结算卡（用时、下一关渐变主按钮）。
2. **限时模式**：倒计时胶囊常驻 HUD，低于 60s 变红；超时触发 TimeoutOverlay（红描边卡 + 再来一局 / 返回），本局不写通关进度。
3. **一键补齐**：useFill() 扣额度 → applySolution(answer)（单 Batch 可一步撤销）→ 版本号自增；额度为 0 按钮灰化。
4. **检查错误**：wrongCells 返回精确错格集合，Canvas 红底红 ✗ 渲染并显示错误数；"清除错误格"批量回未定（可撤销）；玩家任何动手自动清空过期检查结果。
5. **已满足提示变绿**：每次重组计算 satisfiedClues 集合传入 BoardView，数字按"满足→绿、涂黑格→白字、其余→按数值分色"三级渲染。
6. **拖动连涂**：手势循环按半格步长采样移动事件，途经格子逐个回调 strokeCell；笔划在 commit 前不落撤销栈，快速乱拖也只产生一个可撤销单元。
7. **鼠标环境净化**：起笔处检查 PointerType 与按钮，仅 Touch / 左键放行，拖动循环二次过滤；滚轮事件不消费不缩放，桌面与真机行为一致。
8. **关卡数据**：小图 ASCII 字符画、大图程序化作画；Level.fromAnswer 由答案自动生成全量 clueGrid，再叠加 SparseClues 得稀疏谜面。

### 7.3 界面实现

| 界面      | 实现要点                                                                               |
| ------- | ---------------------------------------------------------------------------------- |
| 登录入口页   | 渐变 Hero + 双柔光斑 + 三幅真实关卡像素画玻璃相框（浮动动画）+ 双身份卡（玩家紫 / 管理员金描边）                           |
| 玩家专属页   | 靛紫调深色渐变 + 描边头像徽章 + 通关进度卡（渐变进度条）+ 额度说明 + 渐变"开始游戏"大按钮                                |
| 管理员专属页  | 暖金调深色背景 + 金描边皇冠徽章 + 密码输入（"前往"直接登录，imePadding 键盘避让）+ 金渐变登录按钮                        |
| 首页      | 渐变 Hero 头卡（标题 / 身份与额度玻璃胶囊 / 通关进度 / 退出）+ 模式分段控件 + 关卡网格（通关点亮描边揭晓像素画、未通关深色轮廓"？"、锁定置灰） |
| 游戏页（竖屏） | HUD（返回 / 关名 / 计时胶囊）→ 渐变进度条 → 棋盘（weight 最大化）→ 工具分段控件 → 撤销/重做/重置 → 检查/参考/补齐三键        |
| 游戏页（宽屏） | 左棋盘（封顶 480dp 居中）+ 右侧 230dp 竖排控制栏                                                   |
| 结算      | 胜利卡三色渐变描边 + 撒花 + "下一关"渐变主按钮；超时卡红描边；均随屏高自适应                                         |

***

## 八．系统测试

### 8.1 单元测试（CoreLogicTest，16 项全部通过）

位于 app/src/test/.../model/CoreLogicTest.kt，纯 JVM 运行（`./gradlew testDebugUnitTest` 全绿），按功能点归类如表 8-1：

| 类别    | 用例                               |  结果 |
| ----- | -------------------------------- | :-: |
| 谜面质量  | 10 关循环：每关谜面唯一解且与答案一致             |  ✅  |
| 谜面质量  | 提示推导 3×3 窗口计数正确（全量 / 稀疏，越界裁剪）    |  ✅  |
| 状态语义  | 三态循环（未定→涂黑→留白→未定）                |  ✅  |
| 笔划机制  | strokeCell 目标态写入；已目标态跳过不产生改动     |  ✅  |
| 笔划机制  | 拖涂 N 格 = 撤销栈单元素，undo 一步整笔回退      |  ✅  |
| 笔划机制  | cancelStroke 回滚本笔全部改动            |  ✅  |
| 撤销重做  | 双栈语义；undo 后 redo 恢复；新操作清空重做栈     |  ✅  |
| 胜利判定  | 与答案一致为 true；差一格为 false           |  ✅  |
| 错误检测  | 只报已提交错格，未定格永不误报；清错可撤销            |  ✅  |
| 已满足提示 | 窗口确定且达标 → 满足集合包含该格               |  ✅  |
| 一键补齐  | applySolution 后 isSolved == true |  ✅  |
| 求解器   | maxSolutions=2 时非唯一谜面正确报多解       |  ✅  |

表 8-1 单元测试用例汇总

### 8.2 功能测试（手工 / 模拟器）

在雷电模拟器（1440×2560）及 Resizable 设备上逐项验证，如表 8-2：

| 用例    | 验证点                         |  结果 |
| ----- | --------------------------- | :-: |
| 玩家全流程 | 通关→结算居中→下一关解锁→首页卡片揭晓像素画     |  ✅  |
| 管理员登录 | 错密码提示"密码错误"；admin 登录后全部关卡无锁 |  ✅  |
| 顺序解锁  | 未通关关卡置灰带 🔒，点击无响应           |  ✅  |
| 智能双向笔 | 点空白涂黑、点黑格清除、拖动整笔方向一致        |  ✅  |
| 连涂撤销  | 拖 10 格撤销一步整笔回退，重做恢复         |  ✅  |
| 留白笔   | 浅底圆点标记，不影响胜利判定              |  ✅  |
| 双指缩放  | 缩放平移流畅、双指落下无残留误涂、复位正常       |  ✅  |
| 鼠标净化  | 滚轮 / 右键 / 中键无任何棋盘响应         |  ✅  |
| 检查错误  | 仅错格标红显示错误数，清错一键回未定且可撤销      |  ✅  |
| 已满足变绿 | 完成某提示周围正确涂色后数字变绿            |  ✅  |
| 一键补齐  | 补齐通关、额度 −1、用尽灰化、登录 +2 上限 6  |  ✅  |
| 限时模式  | 超时弹失败卡不写进度，倒计时 <60s 变红      |  ✅  |
| 进度持久化 | 通关 3 关杀进程重启，进度保留第 4 关解锁     |  ✅  |
| 布局适配  | 宽屏切"左棋盘+右侧栏"，结算卡任何屏幕不溢出     |  ✅  |

表 8-2 功能测试用例汇总

### 8.3 关卡数据回归校验与性能观察

`python tools/verify_clues.py` 对全部 10 关输出 OK (unique)，与 Kotlin 单元测试构成双保险（分别校验 Python 侧与 Kotlin 侧的同源数据）。性能方面：28×28 棋盘拖动连涂与双指缩放无肉眼可感卡顿；求解器校验 10 关唯一解总耗时毫秒级；16 项单元测试 JVM 运行不足 2 秒。

***

## 九．遇到的问题、解决办法或改进思路

1. **笑脸嘴角方向错误**：渲染核对发现第 2 关"笑脸"嘴巴画成 ∩ 型（像皱眉）。调整 ASCII 定义两行顺序改为 ∪ 型微笑；图案变化导致原稀疏提示不再唯一解，用 gen\_clues.py 重新生成并写回（31→30 个提示），双端校验恢复唯一解。

2. **月亮孤立杂散像素**：第 9 关月亮（大圆挖偏置圆的程序化作画）在 (5,19)、(19,19) 出现远离月牙主体的孤立像素——大椭圆浮点舍入在边缘填入 0.98<1 的格子，而挖除椭圆在该处 1.03>1 挖不掉。将 carveEllipse 半径 7.5 微调至 7.7 完整覆盖填充边缘后消除，并同步 Python 副本重新生成提示。教训：程序化作画的浮点边界必须显式核对，为此把"终端渲染肉眼核对"固化进工具链流程。

3. **图案双源不同步**：Levels.kt（Kotlin）与 gen\_clues.py（Python）各存一份图案，改一处忘另一处时 verify 会拿旧答案校验新提示产生误判。在流程文档固化"改图案必须两处同步 + 单元测试终审"；长期拟让 Python 直接解析 Kotlin 源码，消除副本实现单一数据源。

4. **鼠标滚轮 / 右键误触发笔划**：桌面调试时滚轮滚动会沿滚动方向"涂出一条笔划"。根源是手势层 awaitFirstDown 不区分指针类型，滚轮事件流被当作拖动。在起笔处增加指针类型与左键过滤（仅 Touch / 左键放行），拖动循环二次过滤，并移除实验性的"滚轮缩放"——滚轮一律无响应，桌面与真机行为完全一致。

5. **三支笔切换繁琐**：初版"涂黑 / 留白 / 橡皮"三支笔，涂擦频繁切换。合并为智能双向涂黑笔：起笔第一格决定整笔方向（数独类 App 成熟范式），一支笔覆盖全部单指操作；笔划机制保证方向判定与一步撤销互不干扰。

6. **胜利结算卡小屏溢出**：结算卡内容总高约 500dp，小屏 / 横屏下超出屏幕被裁切，观感像"没居中"。改 BoxWithConstraints + heightIn(max = maxHeight−24dp) 封顶 + 内容 verticalScroll + 像素画宽度自适应，任何屏幕完整居中。

7. **大棋盘格子太小**：20×20 / 28×28 在竖屏手机格子过小（底部控件占约 310dp）。重排控件（撤销/重做/重置改紧凑胶囊、辅助三键并一行）压缩至约 130dp，棋盘增高约 180dp（16×16 格子从约 16dp 提至 21dp）；宽屏自动切"左棋盘 + 右侧栏"双布局。

8. **改进思路（对接加分项）**：Level 数据结构天然可 JSON 化，接远端关卡仓库（Retrofit + 协程）即实现"游戏从网络获取"；"随机像素图生成（元胞自动机 / 对称掩码）+ 现有求解器唯一解校验"即可无限生成新关卡；引入 Room 存本地成绩做离线排行榜，对战可基于 Firebase 或蓝牙 RFCOMM 同步棋盘事件（笔划 Move 已抽象为可序列化结构）；提示功能直接复用求解器约束传播结果；逻辑层为纯 Kotlin，可经 Kotlin Multiplatform 移植鸿蒙 / 小程序端。

***

## 十．总结

本项目完成了一款规则严谨、交互流畅、界面完整的 Android 原生 Fill-a-Pix 像素填空解谜游戏，主要结论如下：

**1、以"唯一解"为核心的质量观贯穿全程。** 从需求阶段确立"每关谜面必须算法证明唯一解"的硬约束，形成"提示推导 → 贪心去提示 → 求解器校验 → 单元测试把关"的闭环：每次构建时 16 项测试重新证明 10 关唯一解，Python 脚本从另一侧独立回归。这直接回应了康思游戏评分体系中"规则理解是否到位"与"每道游戏是否确定唯一解"两个关键点，也让后续改关（修正笑脸、月亮）有安全网可依赖。

**2、算法能力覆盖"生成"与"求解"两端。** 自研约束传播 + 回溯求解器既是谜面质量闸门，也具备对任意谜面解题的能力；提示推导与稀疏化生成让新关卡制作成本降到"画一幅图 + 跑一次脚本"，具备游戏自动生成管线。

**3、工程分层带来真实的开发效率。** 逻辑层零 Android 依赖使 16 项算法测试毫秒级运行；UI 层问题（滚轮误触、结算溢出、棋盘过小）与逻辑层问题（图案缺陷、提示失效）独立定位、独立修复、独立验证。项目中的多次返工（三支笔 → 智能双向笔、滚轮缩放加入又移除、浅色 → 恒定深色主题）都是真实使用反馈下的小步迭代，体现"可用性优先于炫技"的产品判断。

**4、交互细节决定解谜手感。** 笔划机制（一笔一撤销、可取消）、起笔定向智能笔、双指落下自动回滚误涂、滚轮与右键完全净化、错误检查"默认不打扰"——这些细节共同构成"用笔画画"而非"点格子填表"的核心体验，是本作品区别于普通实现的主要特色。

**5、不足与展望。** 当前版本尚无网络获取、排行榜与对战功能（已列入改进路线，数据结构与算法均已就绪）；导航可进一步引入 Jetpack Navigation 标准化；微信小程序端未实现。后续按"Room 离线排行 → 网络关卡仓库 → 实时对战"顺序逐步补齐。

通过本项目的完整开发周期（需求 → 设计 → 编码 → 测试 → 文档），小组成员切实掌握了移动应用的分层架构设计、Compose 声明式 UI 与状态管理、Canvas 自绘与手势系统、轻量持久化、纯 JVM 单元测试以及配套工具链建设等工程能力，达到了实验目的的各项要求。

***

### 附：项目目录速览

```
xiangsuGame/
├── app/src/main/java/com/example/xiangsugame/
│   ├── MainActivity.kt / GameProgress.kt
│   ├── model/  (CellState, UserRole, GameMode, Level, Levels, SparseClues,
│   │            GameBoard, PuzzleGenerator, PuzzleSolver, Validator)
│   └── ui/     (LoginScreen, HomeScreen, GameScreen, BoardView, Widgets, theme/)
├── app/src/test/.../CoreLogicTest.kt      # 16 项单元测试
├── tools/   (render_patterns.py, final_patterns.py, gen_clues.py,
│             verify_clues.py, update_clues.py, README.md)
├── README.md / 作品说明书.md / 作品说明书2.md
└── gradle/libs.versions.toml
```

